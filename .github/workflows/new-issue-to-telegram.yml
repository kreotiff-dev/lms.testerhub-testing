name: Новый issue → Telegram бот

on:
  issues:
    types: [opened]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Отправить уведомление в чат с ботом
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: html                 # ← вот ключевое изменение!
          disable_web_page_preview: false
          message: |
            <b>🆕 Новый баг/таск в LMS</b><br><br>

            <b>#${{ github.event.issue.number }}</b> — <i>${{ github.event.issue.title }}</i><br>

            👤 Автор: @${{ github.event.issue.user.login }}<br>
            🔗 <a href="${{ github.event.issue.html_url }}">Открыть issue</a><br><br>

            <pre>${{ github.event.issue.body || 'Описание отсутствует' }}</pre><br>

            <i>Создано: ${{ github.event.issue.created_at }}</i>
